%h1 Listing links
%table.ui.selectable.table
  %thead
    %tr
      %th Name
      %th Description
      %th Url
      %th Tags
      %th Expires
      %th Option

  %tbody
    - @links.each do |link|
      %tr
        %td= link.name
        %td= link.description
        %td
          %a{:href => link.url} link
        %td
          .ui.grey.label= display_tag_name(link.tags)
        %td= display_expire_flag(link)
        %td= link_to 'Show', link, :class => "ui mini primary button load"


%br

=paginate(@links)

.ui.divider
%a.ui.primary.button.load{:href => new_link_path}
  %i.plus.icon
  New link

:javascript
  $( ".momentjs" ).each(function() {
    $( this ).html(moment($( ".momentjs" ).text(), "YYYYMMDD").fromNow());
  });
